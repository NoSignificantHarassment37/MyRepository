Antes de arrancar el ejemplo de machine learning, tienen que instalar estas dependencias:

pip install scikit-learn

el paquete tiene estas dependencias:

pip install numpy scipy joblib threadpoolctl
